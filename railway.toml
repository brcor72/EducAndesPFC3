[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate deploy && npx ts-node prisma/seed.ts || true && node dist/main"
healthcheckPath = "/api/v1/courses"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
