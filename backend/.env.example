# Generado automáticamente por Railway — reemplazar con variables reales
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB_NAME

JWT_SECRET=cambia_este_secreto_por_uno_muy_largo_y_seguro_2025
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_DAYS=7

PORT=3001
NODE_ENV=production

# URL del frontend desplegado (sin / al final)
FRONTEND_URL=https://tu-app.vercel.app

# API key de Groq para el Tutor Virtual Yachay (capa gratuita disponible)
# Obtener en: https://console.groq.com/keys
GROQ_API_KEY=COLOCA_TU_API_KEY_DE_GROQ_AQUI

# Token de HuggingFace para embeddings RAG (sentence-transformers, capa gratuita)
# Obtener en: https://huggingface.co/settings/tokens
HF_TOKEN=COLOCA_TU_TOKEN_DE_HUGGINGFACE_AQUI
